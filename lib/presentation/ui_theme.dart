import 'package:flutter/material.dart';

class ArgusColors {
  static const Color primary = Color(0xFF135BEC);
  
  // Dark Mode
  static const Color bgDark = Color(0xFF101622);
  static const Color surfaceDark = Color(0xFF1D2636);
  
  // Light Mode (Matched exactly to HTML)
  static const Color bgLight = Color(0xFFF6F6F8);
  static const Color surfaceLight = Colors.white;
  
  // Text
  static const Color textDark = Color(0xFF0F172A); // slate-900
  static const Color textLight = Color(0xFFF1F5F9); // slate-100
  static const Color slate500 = Color(0xFF64748B);
}
